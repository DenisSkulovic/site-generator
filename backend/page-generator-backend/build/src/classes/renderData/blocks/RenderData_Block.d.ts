import { BlockConfig, BlockContent } from "@page_cls_module";
export declare class RenderData_Block {
    content: BlockContent;
    config: BlockConfig;
    clazz: string;
    constructor(content: BlockContent, config: BlockConfig);
}
