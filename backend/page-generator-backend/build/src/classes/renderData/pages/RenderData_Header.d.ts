import { HeaderConfig, HeaderContent } from "@page_cls_module";
export declare class RenderData_Header {
    content: HeaderContent;
    config: HeaderConfig;
    clazz: string;
    constructor(content: HeaderContent, config: HeaderConfig);
}
